#!/usr/bin/env node

async function main() {
  // tmp npmを利用して仮ディレクトリを作成
  // REPLの起動
  // `use npmName`で仮ディレクトリ内でインストールを行う
  // REPL上で、仮ディレクトリでインストールしたnpmを自由に使うことができる(dayjsのようなライブラリや、当然npxで使えるものも)
  // Ctrl + CでREPLの終了
}

main();
